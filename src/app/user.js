// user.js

//This function models the calls for the user
export default class User {
    constructor(username, password, id) {
      this.username = username;
      this.password = password;
      this.id = id;
    }
  }
  